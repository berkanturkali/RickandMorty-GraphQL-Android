query Character($id: ID!) {
    character(id: $id) {
        id
        name
        status
        species
        type
        image
        gender
        episode {
            id
            name
            created
            air_date
        }
        origin {
            name
        }
        location {
            name
        }
    }
}